# solana-plugin-integrations

This example focuses on the official Solana plugin surfaces.

It demonstrates:

- `solana-node` for native reads and signed transfers
- `solana-trigger` for live-only Solana listeners
- root-owned `plugin_activation` secret bindings
- user-supplied RPC endpoints and commitment-driven confirmation defaults
- raw passthrough and listener surfaces without higher-level protocol workflows
