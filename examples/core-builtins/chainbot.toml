manifest_version = "2.0.0"
chainbot_version = "2.4.0"
profile = "core-builtins"
secret_refs = []

[runtime_defaults]

[paths]

[storage]
mode = "local"

[storage.local]
database_path = "state/runtime.sqlite3"

[storage.raw_debug]
enabled = false
