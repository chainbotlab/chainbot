# builtin-triggers

This example groups the common builtin trigger contracts under one simple workflow.

It demonstrates:

- builtin `manual`
- builtin `market_tick`
- builtin `cron`
- one workflow reusing trigger-derived input from multiple trigger packages
