# workflow-composition

This example focuses on parent and child workflow contracts.

It demonstrates:

- a parent workflow using `kind = "subflow"`
- child workflow input from `subflow_input`
- explicit `subflow_output` return mapping
- builtin-only workflow composition without root-level plugin packages
