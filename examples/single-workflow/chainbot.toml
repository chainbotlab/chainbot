manifest_version = "2.0.0"
chainbot_version = "2.3.3"
profile = "single-workflow"
secret_refs = []

[runtime_defaults]
environment = "example"

[paths]

[storage]
mode = "local"

[storage.local]
database_path = "state/runtime.sqlite3"

[storage.raw_debug]
enabled = false
