# single-workflow

This example shows the smallest standalone ChainBot root.

It demonstrates:

- a canonical `chainbot.toml`
- one workflow package
- one builtin node with workflow defaults
- empty `triggers/`, `plugins/`, `secrets/`, and `state/` placeholders required by the root contract
