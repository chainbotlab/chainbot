# eth-plugin-integrations

This example focuses on the official Ethereum plugin surfaces.

It demonstrates:

- `eth-node` for native reads and signed transfers
- `eth-trigger` for live-only Ethereum listeners
- root-owned `plugin_activation` secret bindings
- user-supplied RPC endpoints instead of a curated network whitelist
- raw passthrough and confirmation defaults without protocol-specific workflow abstractions
