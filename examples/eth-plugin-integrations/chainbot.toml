manifest_version = "2.0.0"
chainbot_version = "2.4.2"
profile = "eth-plugin-integrations"
secret_refs = []

[plugin_activation."eth-node".secret_bindings]
signer = "secret://wallets/eth/hot#private_key"

[plugin_activation."eth-trigger"]
allowed_origins = ["https://rpc.example", "wss://rpc.example"]

[plugin_activation."eth-trigger".secret_bindings]
rpc_token = "secret://providers/ethereum/mainnet#token"

[storage]
mode = "local"

[storage.local]
database_path = "state/runtime.sqlite3"

[storage.raw_debug]
enabled = false
