{
  "skill": "decision-chainbot-plugin-activation-boundary",
  "description": "Routing evals for plugin activation config and envelope boundaries.",
  "cases": [
    {
      "id": 1,
      "prompt": "Add allowed_origins to the activation envelope passed to external node and trigger plugins.",
      "expected_output": "Should load decision-chainbot-plugin-activation-boundary.",
      "files": [],
      "expectations": [
        "Keeps activation root-owned",
        "Preserves dedicated activation envelope semantics"
      ]
    },
    {
      "id": 2,
      "prompt": "Change the cron trigger schedule parser.",
      "expected_output": "Should not load this skill because this is trigger subtype behavior, not plugin activation.",
      "files": [],
      "expectations": [
        "Does not route unrelated trigger params work to activation"
      ]
    },
    {
      "id": 3,
      "prompt": "Change plugin source install staging and revalidation.",
      "expected_output": "Should not load this skill; prefer decision-chainbot-plugin-source-install.",
      "files": [],
      "expectations": [
        "Keeps install lifecycle separate from activation config"
      ]
    }
  ]
}
