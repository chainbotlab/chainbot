# ChainBot V2.1.2 CLI Proposal

Historical proposal document referenced from root `AGENTS.md`.

The original proposal content is not present in this worktree snapshot.
Keep this tombstone path reserved so repository indexes remain accurate until the proposal is restored, archived, or removed from active context.
