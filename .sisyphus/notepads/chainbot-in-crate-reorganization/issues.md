- 2026-03-27: Retired-root dependencies still appear in src/main.rs, src/cli.rs, src/config.rs, src/workflow.rs, src/executor.rs, src/state.rs, src/trigger.rs, src/state_db.rs, src/catalog.rs, src/external_trigger_supervisor.rs, and multiple ingress/builtins files; keep day-one visibility for any surface that those files still import.
- 2026-03-27: Cycle-risk hotspots for the future app/domain/infrastructure split are workflow <-> executor, state <-> config, trigger <-> state/config, ingress <-> state_db/state/config, and builtins nodes <-> workflow/executor/script_protocol/secrets.
- 2026-03-27: Likely minimal verification for task 1 is the surface/contract test set: contract_versions, cli_surface, config_loading, workflow_dag_semantics, execution_scheduler, trigger_plane, ingress_runtime, runtime_state_parity, secrets_runtime, and node_plugin_host.
- 2026-03-27: Tradeoff accepted in Task 1: crate still carries many legacy modules as crate-private roots for compile continuity; structural relocation into `app/domain/infrastructure` is intentionally deferred to later tasks.
- 2026-03-27: Task 2 tradeoff: integration tests still require runtime storage/state setup types; a temporary `app::cli::integration_test_support` re-export bridge was added to keep root-module retirement intact while tests continue to compile.
- 2026-03-27: Rejected Task-2 follow-up confirmed the test-support bridge was not acceptable boundary-wise; tests now directly seed/query SQLite runtime tables, which is narrower but couples fixtures to runtime schema details.
- 2026-03-27: Task 3 accepted a narrow compatibility tradeoff: `src/config.rs` now serves as a crate-private re-export shim to avoid broad internal import churn while ownership is moved to `infrastructure::config` and `app::composition`.
- 2026-03-27: Task 4 keeps a narrow compatibility shim in `src/catalog.rs` (`pub(crate)` re-export only) so immediate call sites do not require broad path churn while ownership is moved to `app::read_model::catalog`.
- 2026-03-27: Additional non-required regression run (`cargo test -p chainbot --test trigger_plane`) still fails because that suite imports crate-private root modules (`chainbot::trigger` and related legacy roots); this is outside Task-5 state split scope but remains a broader root-surface/test migration gap.
- 2026-03-27: Task-5 rejection fix intentionally keeps root-level shim re-exports (`state.rs`/`state_db.rs`) for compatibility; this preserves behavior but still leaves temporary unused-import warnings until later root-compat cleanup.
- 2026-03-27: Task 6 kept `src/executor.rs` as a crate-private shim re-export and added narrow public re-exports (`app::ExecutionPlane`, `domain::runtime` workflow/runtime contract aliases) to keep required integration suites runnable without re-opening private legacy root modules.
- 2026-03-27: Task 7 keeps a narrow staged-compatibility shim in `src/workflow.rs` (`pub use crate::domain::workflow::*`) to avoid broad internal churn while removing logic ownership from the root module.
- 2026-03-27: Task 8 still uses a staged compatibility bridge for trigger-domain ownership: `src/domain/trigger/{contract,emission,acceptance}.rs` currently re-export root `src/trigger.rs` surfaces while runtime supervision was physically moved to `app/runtime/external_triggers`.
- 2026-03-27: Optional broad targeted run (`cargo test -p chainbot <single-test-filter>`) still fails on unrelated integration suites that import private legacy roots (`chainbot::state` in `tests/secrets_runtime.rs` and similar); required Task-8 suites pass after direct test rewires.
- 2026-03-27: Task 8 rejection fix removed the facade-only gap, but a staged tradeoff remains: `domain::trigger::acceptance` still contains process-based external trigger collection internals to preserve current trigger-plane contract tests and behavior; this can be narrowed further in later runtime-boundary cleanup.
- 2026-03-27: Removed runtime artifact drift again by deleting `examples/single-workflow/state/runtime.sqlite3` from the working tree.
- 2026-03-27: Task 8 runtime-boundary rejection fix removed the above tradeoff by relocating process/listener hosting to `app/runtime/external_triggers/process_listener.rs`; acceptance no longer imports process/thread/io/plugin-host runtime management dependencies.
- 2026-03-27: Task-10 helper-text audit still returns two matches in `tests/cli_surface.rs`, but both are intentional negative assertions (`!contains("plugins/manifests/*.toml")`, `!contains("legacy layout detected:")`) and do not indicate live source/runtime dependency on retired layout semantics.
- 2026-03-27: Task 10 resolved the stale AGENTS ownership issue: `crates/chainbot/src/AGENTS.md` now accurately describes the compatibility shim status of retired root modules and points to the real ownership under app/domain/infrastructure.
- 2026-03-27: Task 10 resolved: no implementation docs required modification because their "Files Changed" sections are historical records; stale references in those sections refer to files that still exist as shims, not missing code.

- 2026-03-27: Task 11 blockers / cleanup memo: `src/app/runtime/daemon.rs` still imports `crate::cli::{build_trigger_host_policy, collect_external_trigger_manifests, current_time_ms, execute_single_run, load_replayable_trigger_requests, map_ingress_error, map_runtime_state_error, merge_trigger_requests, normalized_request_from_trigger, RuntimeContext}`, so the root `src/cli.rs` cannot be deleted until those helpers move into the final app-layer home.
- 2026-03-27: Final dependency-direction leak still present: `src/domain/trigger/acceptance.rs` imports `crate::infrastructure::state::{sanitize_path_component, RuntimeStateError, RuntimeStateStore}`. Domain-trigger acceptance is still coupled to infrastructure state internals and should be narrowed behind a domain contract or moved out of `domain` before root deletion is considered complete.
- 2026-03-27: Temporary alias bridges that must disappear with retired roots: `src/config.rs`, `src/state.rs`, `src/state_db.rs`, `src/workflow.rs`, `src/trigger.rs`, `src/executor.rs`, `src/catalog.rs`, `src/external_trigger_supervisor.rs`, and `src/trigger_wasm.rs` are still compatibility re-export shims.
- 2026-03-27: Additional boundary risk to watch: `src/domain/trigger/contract.rs` still calls `crate::builtins::triggers::validate_builtin_trigger_definition`; this is not a root-module dependency, but it is a domain-to-runtime-facade coupling that should be reviewed if the final direction is meant to keep domain purely contract-oriented.
- 2026-03-27: Recommended verification commands for Task 11: `cargo check -p chainbot && cargo test -p chainbot --lib`, `rg "^(pub mod (cli|config|executor|external_trigger_supervisor|state(_db)?|trigger(_wasm)?|workflow));$" crates/chainbot/src`, `rg "crate::cli::|crate::config::|crate::workflow::|crate::trigger::|crate::state::|crate::state_db::|crate::executor::|crate::external_trigger_supervisor::|crate::trigger_wasm::|crate::catalog::" crates/chainbot/src crates/chainbot/tests`.

- 2026-03-27: Final-wave regex audit command `rg "^(\s*mod|\s*pub mod) (cli|catalog|config|executor|external_trigger_supervisor|state(_db)?|trigger(_wasm)?|workflow);$" crates/chainbot/src` still matches submodule declarations (`domain/mod.rs`, `infrastructure/mod.rs`, `builtins/*/mod.rs`, `app/mod.rs`); this is a broad pattern artifact and not evidence of retired root shims.
- 2026-03-27: `cargo check -p chainbot` is green but reports existing dead-code warnings in external trigger runtime files; no new compile blocker was introduced by final-wave fixes.
- 2026-03-27: Reviewer concern about extra sibling file resolved: `app/cli_impl.rs` removed from tree; runtime implementation now sits under `app/cli/`.
- 2026-03-27: Remaining compiler warnings are unchanged dead-code warnings in external trigger runtime modules and are outside this no-behavior reviewer-hardening scope.
- 2026-03-27: CLI split risk hotspot was cross-file `impl CliRequest` visibility; mitigated by keeping shared types in `mod.rs`, parse-only methods in `parse.rs`, dispatch/execute methods in `commands.rs`, and help rendering in `help.rs`.
- 2026-03-27: `.sisyphus/boulder.json` initially appeared as deleted (`D`) from prior cleanup; resolved by restoring tracked file state so reviewer audit command returns empty output.
- 2026-03-27: F1 final blocker was doc/comment truthfulness after root shim deletion: three implementation docs still read as if `src/state_db.rs`, `src/state.rs`, `src/cli.rs` were active current locations rather than deleted files; fixed with minimal append-only changes that mark references as historical.
