- Task 1 current root surface is still legacy: `lib.rs` exports `builtins`, `catalog`, `cli`, `config`, `errors`, `executor`, `external_trigger_supervisor`, `ingress`, `plugin`, `script_protocol`, `secrets`, `state`, `state_db`, `trigger`, `trigger_wasm`, and `workflow`.
- `main.rs` still routes through `chainbot::cli::run_from_env()` and maps success/error to `chainbot::errors::CliExitCode`.
- Immediate breakage set for the root-surface cut is concentrated in `crates/chainbot/src/main.rs` and integration tests importing retired root modules: `cli`, `config`, `workflow`, `executor`, `trigger`, `state`, `state_db`, and `external_trigger_supervisor`.
- A minimal skeleton will need new `app/mod.rs`, `domain/mod.rs`, and `infrastructure/mod.rs` files plus a stub `app::cli::run_from_env()` before the public surface can compile.
- 2026-03-27: Task 1 can freeze the public surface without consumer migration by switching retired root modules in `lib.rs` from `pub mod` to crate-private `mod`; this preserves internal `crate::...` references while removing external exports immediately.
- 2026-03-27: `app::cli::run_from_env()` should avoid exposing `crate::cli::CliOutput` directly because the legacy `cli` module is crate-private; a small public `app::cli::CliOutput` wrapper keeps the boundary stable and compiles cleanly.
- 2026-03-27: Task 2 can make `app::cli` the real parser/help/dispatch owner by moving `CliRequest` parsing helpers and help rendering there while keeping runtime-heavy command executors as `impl CliRequest` methods in legacy `cli.rs`.
- 2026-03-27: Integration tests that previously imported retired root modules (`chainbot::config/state/state_db`) need a narrow bridge; re-exporting required runtime types through `chainbot::app::cli::integration_test_support` keeps tests compiling without reopening retired root modules.
- 2026-03-27: Public test-support re-exports under `app::cli` are boundary scope creep for Task 2; the safer fix is test-local SQLite persistence helpers that seed/query runtime tables without changing public module ownership.

- 2026-03-27: Task 3 cut map for config loading: `root_layout` should own `DEFAULT_ROOT_DIR_NAME`, `CHAINBOT_CONFIG_DIR_ENV`, `ROOT_CONFIG_FILE_NAME`, `RootLayout`, `resolve`, `resolve_with_env_home`, `from_root`, `root_config_path`, `validate_bootstrap_paths_exist`, `validate_paths_exist`, and `apply_root_config`; `package_loader` should own `decode_required_toml`, `validate_directory_exists`, `validate_file_exists`, `collect_package_config_files`, `decode_package_collection`, `decode_plugin_manifests`, `collect_plugin_package_config_files`, `resolve_root_relative_dir`, and the `PackageManifestExt` helper impls; `loader` should own `resolve_root_layout`, `load_effective_root_layout`, `reconcile_root_config_version`, `run_root_config_version_migration`, `persist_root_config_version`, `RootDefinitionBundle::load`, `set_trigger_enabled`, `load_trigger_definitions`, `validate_bundle_contracts`, and `validate_package_identity`.
- 2026-03-27: Types that should stay infrastructure-owned for Task 3 are the root/storage contract types: `StorageMode`, `StorageDefinition`, `LocalStorageDefinition`, `PostgresStorageDefinition`, `RawDebugArtifactsDefinition`, `RuntimeHistoryRetentionDefinition`, `RuntimeStorageBackend`, `RuntimeHistoryRetentionPolicy`, `RuntimeStorageConfig`, `ConfigRoot`, `WorkerTemplate`, `RootConfigDefinition`, `RootPathOverrides`, `RootDefinitionBundle`, `TriggerToggleResult`, and `RootLayout`.
- 2026-03-27: Immediate call sites that need import rewiring after the split are `crates/chainbot/src/cli.rs` (`execute_init`, `execute_trigger`, `resolve_existing_root`, `resolve_trigger_root`, `load_catalog_plugins_strict`, `load_catalog_plugins_if_available`) plus integration tests in `crates/chainbot/tests/config_loading.rs` and `crates/chainbot/tests/contract_versions.rs`.
- 2026-03-27: Verification focus for Task 3 should stay on loader semantics, not runtime/state changes: `cargo test -p chainbot --test config_loading` and `cargo test -p chainbot --test contract_versions` are the most relevant checks, with special attention to root-layout resolution, version backfill/migration gating, package discovery, manifest validation, and trigger/package loader behavior.
- 2026-03-27: Task 3 implementation split landed with `infrastructure::config::{root_layout,package_loader,loader}` owning path decode/loading side effects while `app::composition` now owns root bundle assembly and bundle-wide validation.
- 2026-03-27: `validate_bundle_contracts()` moved as one unit into `app::composition::validate`, including ingress desired-state checks and package-directory identity checks for workflow/trigger/plugin packages.
- 2026-03-27: Required validation for Task 3 passed after rewiring to the composition entrypoint: `cargo test -p chainbot --test config_loading` and `cargo test -p chainbot --test contract_versions`.

- 2026-03-27: Task 4 cut line is clear: `src/catalog.rs` should become `app::read_model::catalog`, taking all catalog list/show models, plugin summary views, detail builders, and text renderers with it.
- 2026-03-27: `src/cli.rs` still owns the orchestration shell for `status`, `observe`, and `catalog` (root resolution, bundle/state-store loading, json/text selection, and error mapping), but not the read-model structs/builders/renderers.
- 2026-03-27: Status read-model candidates in `cli.rs` are `StatusOutput`, `StatusRootView`, `StatusServeView`, `StatusWorkflowView`, `StatusTriggerView`, `StatusSummaryView`, `build_status_output`, `render_status_output`, and `render_serve_lease_state`; observe candidates are `ObserveOutput`, `ObserveSummaryView`, and `render_observe_output`.
- 2026-03-27: CLI-owned helpers that should remain in `app::cli` are the command dispatchers, `load_catalog_plugins_if_available/strict`, and the trigger renderers (`render_trigger_list_output`, `render_trigger_operation_output`).
- 2026-03-27: Best regression set for Task 4 is `cargo test -p chainbot --test cli_surface status_prints_human_summary_for_basic_root observe_json_reports_recent_runtime_history repeated_status_json_reads_remain_read_only` plus `cargo test -p chainbot --test catalog_surface`.
- 2026-03-27: Task 4 landed with explicit app-layer ownership: catalog list/show and plugin summary read-models now live in `app::read_model::catalog`, status/observe payload builders and renderers live in `app::read_model::{status,observe}`, and CLI command handlers call those modules without changing output contracts.
- 2026-03-27: Serve/stop/internal-daemon orchestration moved into `app::runtime::daemon`; ingress supervisor and external trigger supervisor remain dependency calls inside daemon flow rather than being rehomed.
- 2026-03-27: Runtime lifecycle tests in `cli.rs` can stay in place by importing `app::runtime::daemon::{ServeLeaseSupervisor, serve_once_with_lease, teardown_external_trigger_sessions}` directly, avoiding broader test fixture churn in Task 4.
- 2026-03-27: Rejected Task-4 report on `repeated_status_json_reads_remain_read_only` is resolved in current tree: `execute_status` reads persisted `run_summaries` and `app::read_model::status::build_status_output` computes `summary.run_count` directly from `run_summaries.len()`, so repeated JSON reads stay read-only and stable.
- 2026-03-27: Task 5 extracted backend-agnostic runtime-state contracts into `src/domain/state/mod.rs` (run/trigger/inbox/staged/checkpoint/snapshot/lease models plus pure snapshot-token helpers), and file/db/coordination callers now consume those domain types through `infrastructure::state` imports.
- 2026-03-27: Required runtime-state verification suites for Task 5 passed after rewiring (`state_runtime_persistence`, `runtime_state_parity`, `runtime_guardrails`), preserving lease, staged ordering, replay suppression, and retention/archive behavior.
- 2026-03-27: Task 5 follow-up replaced infrastructure facades with real ownership: file-backed persistence now lives in `src/infrastructure/state/file_store.rs`, SQLite coordination now lives in `src/infrastructure/state/sqlite_coordination.rs`, and DB-primary runtime store now lives in `src/infrastructure/state/db_store.rs`.
- 2026-03-27: Legacy roots `src/state.rs` and `src/state_db.rs` are now compatibility shims only; they re-export domain/infrastructure surfaces without owning persistence logic.
- 2026-03-27: Task 6 split landed with runtime contracts moved to `src/domain/runtime/mod.rs` (`NodeDefinition`, `NormalizedRunRequest`, `ScheduledNodeState`, `WorkflowRunStatus`, `WorkflowRunReport`, plus validation/constructor/terminal helpers), while concrete orchestration moved to `src/app/runtime/execution.rs`.
- 2026-03-27: `workflow.rs`, builtin dispatch/registry, and CLI run paths now consume domain runtime contracts and app execution entrypoints instead of owning scheduler logic inside `src/executor.rs`.
- 2026-03-27: Required semantic suites passed after Task 6 rewires: `cargo test -p chainbot --test execution_scheduler` (11 passed) and `cargo test -p chainbot --test workflow_dag_semantics` (7 passed).
- 2026-03-27: Task 7 moved workflow semantic ownership to `src/domain/workflow/{contract,variables,when,subflow}.rs`; `src/workflow.rs` is now a thin compatibility re-export only.
- 2026-03-27: Direct consumers (`cli`, `infrastructure::config`, `app::composition`, `app::runtime::execution`, `app::read_model::status`, builtin node contract, and workflow/scheduler suites) now import `domain::workflow` directly, preserving DAG/namespace/when/subflow behavior unchanged.
- 2026-03-27: Required Task-7 verification passed after import rewires: `cargo test -p chainbot --test workflow_dag_semantics` (7 passed) and `cargo test -p chainbot --test execution_scheduler` (11 passed).
- 2026-03-27: Task 8 moved long-lived external trigger runtime ownership by relocating `src/external_trigger_supervisor.rs` and `src/trigger_wasm.rs` into `src/app/runtime/external_triggers/{supervisor,wasmtime}.rs`, with root modules reduced to compatibility re-exports.
- 2026-03-27: Task 8 established `src/domain/trigger/{mod,contract,emission,acceptance}.rs` and rewired trigger-related callers (`cli`, `app/runtime/daemon`, ingress modules, builtins trigger modules, composition/config/read-model imports, and `tests/trigger_plane.rs`) to `domain::trigger` paths.
- 2026-03-27: Required Task-8 verification commands passed after rewires: `cargo test -p chainbot --test trigger_plane` (19 passed) and `cargo test -p chainbot --test ingress_runtime` (4 passed).
- 2026-03-27: Task 8 rejection fix moved real logic ownership out of `src/trigger.rs`: contract types/validation now live in `domain/trigger/contract.rs`, emission structures/mapping in `domain/trigger/emission.rs`, and trigger-plane acceptance/dedup/cooldown/restart suppression plus process trigger collection in `domain/trigger/acceptance.rs`.
- 2026-03-27: Required regression set still passes after the real move: `cargo test -p chainbot --test trigger_plane`, `cargo test -p chainbot --test ingress_runtime`, and `cargo check -p chainbot`.
- 2026-03-27: Task 8 follow-up extracted process/listener hosting code from `domain/trigger/acceptance.rs` into `app/runtime/external_triggers/process_listener.rs`; acceptance now only keeps domain acceptance/suppression/normalization logic plus runtime-state persistence writes.
- 2026-03-27: Trigger-plane behavior parity was preserved by wiring a shim-level external emission collector in `src/trigger.rs` so plugin manifest validation still fails at open time while process listener protocol failures still surface during `collect_run_requests`.
- 2026-03-27: Task 9 consumer migration scope reduced to one real first-party file: `crates/chainbot/tests/secrets_runtime.rs`; no additional retired-root matches were found under `examples/`.
- 2026-03-27: `secrets_runtime.rs` now imports final module tree paths only: `infrastructure::config::RootLayout`, `domain::state::{RunRecordSummary, RunStatus, WorkflowRuntimeLogEntry}`, and `infrastructure::state::{CoordinationStore, FileBackedStateStore, StateLayout}`.
- 2026-03-27: Required Task-9 verification matrix passed after import rewiring (`secrets_runtime`, `config_loading`, `execution_scheduler`, `trigger_plane`, `runtime_state_parity`, `state_runtime_persistence`, `worker_host`, `cli_surface`) and retired-root import audit returned zero relevant matches.
- 2026-03-27: Task 10 rewired the last real `crate::config` callsites to stable paths: builtin node context/registry now use `infrastructure::config::RootLayout`, ingress supervisor uses `infrastructure::config::RuntimeStorageConfig`, and external trigger supervisor tests use `infrastructure::config::{RuntimeStorageBackend, RuntimeStorageConfig}`.
- 2026-03-27: Source/helper shim text no longer references retired root module paths (`crate::catalog`, `crate::config`, `crate::workflow`) and no longer points operators to `~/.chainbot/config/root.toml`; the root-config error hint now only references `~/.chainbot/chainbot.toml` plus `CHAINBOT_CONFIG_DIR`.
- 2026-03-27: Required Task-10 verification passed unchanged: shim-import audit returns zero matches, helper-text audit reports only two negative-assertion checks in `tests/cli_surface.rs`, and test suites `--lib`, `--test cli_surface`, and `--test trigger_plane` all pass.
- 2026-03-27: Task-10 rejection fix removed untracked runtime artifact drift by deleting `examples/single-workflow/state/runtime.sqlite3`; post-verification `git status -- examples/single-workflow/state/runtime.sqlite3` remains clean.
- 2026-03-27: Task 10 final sync updated `crates/chainbot/src/AGENTS.md` with accurate shim ownership table: catalog->app::read_model::catalog, cli->app::cli, config->infrastructure::config, workflow->domain::workflow, trigger->domain::trigger, executor->app::runtime::execution, state->domain::state+infrastructure::state, state_db->infrastructure::state, external_trigger_supervisor->app::runtime::external_triggers, trigger_wasm->app::runtime::external_triggers::wasmtime.
- 2026-03-27: Task 10 added required inline ASCII diagrams: app/composition/mod.rs (layout->loaders->validation->bundle seam), domain/state/mod.rs (record/snapshot/token seam), app/runtime/execution.rs (NormalizedRunRequest->waves->dispatch->report), domain/trigger/acceptance.rs (domain acceptance vs app supervision boundary).
- 2026-03-27: Task 10 stale-reference audit (grep pattern for retired root module paths in AGENTS/docs/examples) returns zero matches; `cargo test -p chainbot --test contract_versions` passes.
- 2026-03-27: Task 10 note: triple backticks inside Rust doc comments (`//! ```text`) cause LSP parsing errors in some configurations; using `//` style comments for ASCII diagrams avoids this issue.
- 2026-03-27: Task 11 removed all retired root shim files from `crates/chainbot/src/` (`catalog.rs`, `config.rs`, `executor.rs`, `external_trigger_supervisor.rs`, `state.rs`, `state_db.rs`, `trigger.rs`, `trigger_wasm.rs`, `workflow.rs`, `cli.rs`) and cleaned `lib.rs` to the final public module tree only.
- 2026-03-27: Daemon-only runtime helpers (`RuntimeContext`, `execute_single_run`, trigger merge/replay mapping, runtime-state/ingress error mappers, and clock helpers) now live in `app::cli_runtime`; `app::runtime::daemon` imports `crate::app::cli_runtime::*` so deleting `src/cli.rs` no longer blocks daemon lifecycle flows.
- 2026-03-27: `domain::trigger::acceptance` no longer imports `infrastructure::state`; it now depends on a domain-owned `TriggerStateStore` contract and a domain-owned `sanitize_path_component`, with infrastructure adapting `RuntimeStateStore` via trait impls in `infrastructure::state::mod`.
- 2026-03-27: TriggerPlane compatibility constructors (`open`, `open_with_store`, `open_with_store_acceptance_only`, `open_legacy_state_layout_for_tests`) were preserved by moving their impl block to `app::runtime::trigger_plane`, keeping process-listener/plugin-host ownership in app runtime while domain acceptance stays contract-oriented.
- 2026-03-27: Required Task-11 verification matrix passed end-to-end after shim deletion (`cargo check -p chainbot`, `cargo test -p chainbot --lib`, and all requested integration suites including `trigger_plane`, `ingress_runtime`, `cli_surface`, and `end_to_end_vertical_slice`).
- 2026-03-27: Final-wave verification confirms external-trigger staging now persists raw payloads (`daemon` process staging and `external_triggers::supervisor` wasm staging), while payload mapping stays in domain acceptance.
- 2026-03-27: Added nested-selector regression coverage in `tests/trigger_plane.rs` (`payload.quote.symbol`) so acceptance output fails if any pre-acceptance double mapping is reintroduced.
- 2026-03-27: `app/runtime/trigger_plane.rs` is removed and TriggerPlane compatibility constructors now live in `app/runtime/mod.rs`; `app/cli_runtime.rs` remains removed.
- 2026-03-27: Monolithic domain modules were split to declaration/re-export `mod.rs` files: `domain/state/{model,lease,records}.rs` and `domain/runtime/{contract,report}.rs`.
- 2026-03-27: Reviewer-hardening layout fix replaced sibling `app/cli_impl.rs` with plan-friendly `app/cli/runtime.rs` under directory module `app/cli/mod.rs` while keeping `app::cli` public path unchanged.
- 2026-03-27: CLI/runtime contract behavior remained stable after layout fix; required checks passed: `cli_surface`, `end_to_end_vertical_slice`, `contract_versions`, `cargo check -p chainbot`, and legacy-root import audit grep.
- 2026-03-27: Implementation docs can keep historical sections append-only while still correcting top-level current-state ownership notes to avoid contradictions after shim deletion.
- 2026-03-27: Final F1 closure required a literal CLI tree split: parser logic moved to `app/cli/parse.rs`, help rendering moved to `app/cli/help.rs`, and execution/runtime glue moved from `runtime.rs` to `commands.rs` while preserving `app::cli` API/behavior.
- 2026-03-27: Keeping daemon-facing helper re-exports at `app::cli` facade (`mod.rs`) avoids import churn in `app/runtime/daemon.rs` during structural-only refactors.
- 2026-03-27: F4 closure for `.sisyphus/boulder.json` is achieved by restoring tracked file state so path-local `git status --short .sisyphus/boulder.json` is empty.
- 2026-03-27: Final F1 closure required minimal append-only doc fixes: changed present-tense location claims in implementation doc bullets to historical framing with "原...已删除" or "formerly in...now in" so they no longer read as active current locations.
