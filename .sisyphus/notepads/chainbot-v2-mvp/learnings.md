- 2026-03-16: Freeze V2 module boundaries in crates/chainbot/src with explicit modules for cli/config/workflow/state/trigger/executor/plugin/worker/secrets/errors and public export through lib.rs.
- 2026-03-16: Keep version compatibility checks centralized via assert_supported_major and enforce mandatory schema_version/api_version/protocol_version through required serde fields plus explicit validation.
- 2026-03-16: Integration test names contract_versions and invalid_contract_fixtures_are_rejected are used as command filters, so keep those exact names stable for orchestrator verification.
- 2026-03-16: Root layout validation is most stable when represented as an explicit RootLayout value object (root/config/workflows/triggers/plugins/secrets/state) instead of ad-hoc path joins scattered across CLI and loaders.
- 2026-03-16: A deterministic TOML loader pattern (sorted *.toml discovery + typed per-file decode errors that include file path) keeps fixture validation reproducible and gives actionable failure messages.
- 2026-03-16: AGENTS metadata must be updated in the same task as behavior changes; stale module descriptions are treated as QA failures even when code and tests pass.
- 2026-03-16: Runtime state is easier to keep deterministic when the state tree uses fixed paths (`state/runs`, `state/workflow-logs`, `state/trigger-records`) and every file write is staged with `.next` plus atomic rename.
- 2026-03-16: Recovery behavior is testable and stable when startup treats staged-only run summaries as promotable and staged+committed pairs as stale temp files that should be removed.
- 2026-03-16: DAG validation stays deterministic when dependency checks normalize (`sort + dedup`) each node's `depends_on` list before graph edge insertion, and cycle reports return sorted node ids.
- 2026-03-16: Runtime variable precedence is easiest to freeze as a dedicated value object (`RuntimeVariableLayers`) that resolves keys through fixed-source order (`CLI > manual > trigger > workflow > config`) using ordered maps/sets.
- 2026-03-16: Keep workflow semantic validation ordered as workflow-scoped checks first, then node-level checks, to avoid leaking placeholder workflow ids in user-facing semantic errors.
- 2026-03-16: Keep the CLI surface deterministic by separating stdout payloads from typed `UserFacingError` values; `--help` and data commands print to stdout, while usage/validation/state failures map to explicit non-zero exit codes.
- 2026-03-16: Trigger-plane startup should validate trigger-definition shape (non-empty ids/source, known kind aliases, duplicate trigger_id rejection) before any dispatch loop so misconfiguration fails fast and deterministically.
- 2026-03-16: Run-request normalization should include time components in generated run ids and persist trigger-record sequence from acceptance order to avoid file-path collisions across repeated trigger events.
- 2026-03-16: Trigger kind modeling must treat only explicit plugin aliases (`external_plugin`, `external_trigger`, `plugin`) as external host dispatch; all other trigger kinds remain builtin-compatible (for example `manual`, `market_tick`) to preserve CLI/config validation fixtures.
- 2026-03-16: Execution-plane scheduling stays deterministic when ready nodes are emitted in sorted wave batches (`schedule_waves`) while dependency/when evaluation remains explicit in Rust state transitions.
- 2026-03-16: Preserve runtime variable precedence in scheduler runtime by materializing `run_scoped` from `RuntimeVariableLayers::resolve` (`CLI > manual > trigger > workflow > config`) before node input bindings run.
- 2026-03-16: Pass-style secret resolution is safest when `secret://namespace/path#key` maps to `<secrets_root>/namespace/path.gpg`, rejects `.`/`..` path segments, and decrypts only the referenced secret at runtime.
- 2026-03-16: Redaction guarantees are easier to verify by centralizing `redact_text` and `redact_json_snapshot`, then asserting persisted state artifacts never contain resolved plaintext tokens.
- 2026-03-16: External trigger plugins must pass both generic plugin contract validation and host policy validation (allowlist + required capability + root-confined executable path) before any process spawn.
- 2026-03-16: Trigger-plane normalization is easiest to verify when accepted events return a run-request payload that includes the persisted trigger-record path for deterministic file assertions.
- 2026-03-16: External node plugin spawning should happen only after canonical path containment (`plugins_root`), executable-bit checks, capability subset checks, and input schema key validation to guarantee reject-before-spawn behavior.
- 2026-03-16: Subprocess worker hosting is safest with `Command::env_clear()` plus explicit per-worker env allowlist, request JSON over stdin, and strict stdout/stderr byte caps checked against total streamed bytes.
- 2026-03-16: Task-11 verification is stable when CLI lazily materializes `target/test-roots/e2e` from deterministic fixture snapshots if the path is missing, so `validate --root target/test-roots/e2e` is agent-runnable without manual setup.
- 2026-03-16: End-to-end fixtures should separate success and failure roots (`e2e` vs `e2e-failure`) to avoid cross-test state pollution in required post-test `list-runs` verification.
- 2026-03-16: Restart-only reload policy is easiest to make explicit when each CLI invocation snapshots definitions once; on-disk TOML mutations must not affect an already loaded runtime, and only a fresh `load_runtime_context` observes the change.
- 2026-03-16: Duplicate-trigger suppression after restart can stay file-backed by rebuilding an accepted-event set from persisted trigger records (`trigger_id + event_id`) before any new trigger emission normalization.
- 2026-03-16: Final scope-fidelity audit confirms all MVP pillars are present end-to-end (CLI-first command surface, external trigger/node plugin hosts, subprocess script workers, pass/PGP secret resolution with redaction, file-backed runtime artifacts, and SQLite limited to lease + dedup/cooldown coordination).
- 2026-03-16: F1 plan-compliance audit approved when direct file evidence showed no HTTP expansion, SQLite stayed limited to lease/dedup coordination, runtime artifacts stayed file-backed, and deterministic e2e fixtures plus versioned plugin/worker/secret boundaries covered tasks 1-12.
- 2026-03-16: A post-fix F2 rerun approved once three guardrails were simultaneously visible in code and tests: `serve` drains every accepted request from a trigger snapshot, unknown trigger kinds fail validation explicitly, and trigger coordination can be rebuilt from file-backed trigger records after restart.
- 2026-03-16: When trigger acceptance can yield more than one normalized request, `serve` must either drain the entire accepted batch or persist an explicit queue before acknowledging any event; consuming only the first request creates permanent work loss.
- 2026-03-16: In mixed SQLite + file-backed trigger pipelines, suppressive dedup/cooldown tokens cannot be committed before the durable trigger record, or restart recovery will see coordination state that has no file-backed acceptance evidence.
- 2026-03-16: Re-review evidence is strongest when each resolved blocker is backed by both source-line changes and one targeted regression command, so approvals do not rely on static inspection alone.
