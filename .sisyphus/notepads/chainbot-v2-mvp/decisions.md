- 2026-03-16: Model contract versions as string fields with explicit validation functions to keep serialized wire format simple while still returning typed ContractError variants.
- 2026-03-16: Introduce src/lib.rs to expose contract modules for integration tests; keep main.rs as minimal bootstrap only, with runtime behavior intentionally deferred.
- 2026-03-16: Reject unsupported future major versions (>1) for config schema, workflow/trigger/node/plugin API contracts, and worker protocol contracts to lock MVP boundary without adding V1 compatibility shims.
- 2026-03-16: Keep Task 1 JSON contract parsing APIs intact (for contract freeze regression tests) and add Task 2 TOML root loading as a parallel path via RootDefinitionBundle so both contract tests and root-layout tests remain valid.
- 2026-03-16: Treat `secret://...` values as opaque references by validating syntax only at config-load time and deferring any secret-file reads/decryption to future tasks.
- 2026-03-16: Scope SQLite coordination to two tables only (`serve_leases`, `coordination_tokens`) and keep run summaries/workflow logs/trigger records file-backed to preserve the plan's storage boundary.
- 2026-03-16: Separate runtime state failures into typed boundaries (`CoordinationError` for SQLite operations and `FileStateError` for file-backed persistence/recovery) so callers can route retry/recovery behavior explicitly.
- 2026-03-16: Introduced explicit workflow-semantic types in `workflow.rs` (`DependsMode`, `WhenCondition`, `RuntimeVariableNamespace`, `SubflowContract`) and kept scheduler behavior out of this module; validation-only DAG assistance uses `petgraph`.
- 2026-03-16: Enforced subflow isolation contract by modeling imports/exports as explicit mappings and exposing boundary helpers (`build_child_inputs`, `collect_exports`) that copy only declared values across parent/child scopes.
- 2026-03-16: Model the V2 CLI around a small manual parser instead of adding a new dependency so command names, help text, root override behavior, and exit-code mapping stay fully explicit and stable for orchestrated verification.
- 2026-03-16: Treat trigger-definition kind aliases (`plugin`, `external_trigger`) as `external_plugin` at validation time to keep manifest contracts backward-compatible while preserving a single runtime dispatch category.
- 2026-03-16: Keep scheduler authority in `executor.rs` with explicit node states (`pending/blocked/ready/running/succeeded/skipped/failed`) and wave-based ready-node planning so plugin/script engines remain pure dispatch targets.
- 2026-03-16: Standardize builtin node dispatch on a registry contract (`BuiltinNodeRequest` -> `BuiltinNodeResult`) and reserve typed `UnknownBuiltinNodeKind` failures for unsupported builtin kinds.
- 2026-03-16: Implement runtime secret resolution behind a `SecretDecryptor` seam and keep production decryption in `GpgSecretDecryptor`, so tests can use deterministic fake decryptors without requiring a global `gpg` installation.
- 2026-03-16: Keep secret caching boundary at "no shared cache" for now; resolve only requested references and return redacted decryption failures that include metadata (reference/path) but never plaintext.
- 2026-03-16: Keep trigger-plane plugin safety checks local to `trigger.rs` (allowlist, capability subset, executable path confinement, API compatibility) and reuse `CoordinationStore` + `FileBackedStateStore` instead of adding a new persistence subsystem.
- 2026-03-16: Keep plugin kind parsing backward-compatible by accepting both explicit V2 kinds (`external_node`, `external_trigger`) and legacy aliases (`node`, `trigger`) while enforcing node-plugin execution only on the external-node branch.
- 2026-03-16: Added `WorkerHost` with `WorkerHostLimits` and `WorkerProcessSpec` to keep scheduler authority in Rust while script workers remain pure subprocess protocol executors with timeout kill+wait cleanup.
- 2026-03-16: Keep `serve` trigger-plane manifest input restricted to external-trigger plugin manifests only; passing mixed plugin kinds into trigger host validation causes false policy rejections for external-node plugins.
- 2026-03-16: Implement task-11 e2e fixtures as full root-layout snapshots under `crates/chainbot/tests/fixtures/e2e/{success,failure_missing_secret}` and copy them into `target/test-roots/*` at runtime for deterministic command/test reproducibility.
- 2026-03-16: Enforce append-only workflow-log and trigger-record paths by rejecting committed-path overwrites, then recover `.next` artifacts at startup by promoting staged-only files and removing stale staged duplicates.
- 2026-03-16: Treat a live `serve` process as "restart-safe" only when lease reacquisition can ignore dead PID owners and incomplete runs are rewritten to failed summaries with an explicit recovery log entry.
