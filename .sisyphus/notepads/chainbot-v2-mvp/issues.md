- 2026-03-16: Initial secret parser only accepted exactly two path segments (`secret://namespace/name`) and rejected `secret://ops/slack/webhook`; updated parser to allow hierarchical secret names after namespace while still rejecting empty segments.
- 2026-03-16: Trigger IDs and event IDs can include path separators (`/`) or symbols, so file-backed trigger-record naming must sanitize components to avoid accidental nested paths while keeping deterministic mapping.
- 2026-03-16: Node-level validation currently runs before workflow context is attached, so executor-local `InvalidVariableReference` errors use workflow_id `<unknown>` and are then superseded by workflow-scoped validation; keep this in mind if reusing node validation standalone.
- 2026-03-16: The reusable `target/test-roots/basic` fixture can accumulate state between runs, so CLI integration tests must reset `state/runs`, runtime log directories, and the coordination SQLite file before asserting an empty `list-runs` response.
- 2026-03-16: Trigger contract validation now enforces explicit kinds (`builtin` or external plugin aliases), so scheduler-focused fixtures must avoid legacy `kind = "manual"` values or workflow validation will fail before scheduling starts.
- 2026-03-16: `PluginManifest::validate()` now enforces non-empty `plugin.executable` for external trigger plugins, so trigger-plane fixtures must set `executable` even when `entrypoint` is present.
- 2026-03-16: Workspace verification for Task 8 surfaced pre-existing compile blockers in `trigger.rs`, `trigger_plane.rs`, and `errors.rs`; minimal compatibility fixes were required so worker-task validation commands could run.
- 2026-03-16: Initial task-11 `serve` wiring passed all plugin manifests into trigger-host policy validation; this incorrectly rejected external-node manifests as non-allowlisted trigger plugins and required manifest filtering to external-trigger kind before opening `TriggerPlane`.
- 2026-03-16: Final F2 review found `crates/chainbot/src/cli.rs` persists/normalizes all accepted trigger events but executes only the first request in `serve`, so multi-event snapshots can drop accepted work permanently.
- 2026-03-16: Final F2 review found `crates/chainbot/src/trigger.rs` still treats unknown `trigger.kind` values as builtin-compatible instead of returning `UnknownTriggerKind`, so `validate` does not fail fast on trigger-kind typos.
- 2026-03-16: Final F2 review found `crates/chainbot/src/trigger.rs` claims dedup/cooldown coordination before durable trigger-record persistence, allowing restart-time suppression without any file-backed acceptance record.
