
- Task 1/4 evidence: contract/runtime split lives in `crates/chainbot/src/plugin/contract.rs`, config loading in `crates/chainbot/src/config.rs`, short-lived trigger lifecycle in `crates/chainbot/src/trigger.rs` + `crates/chainbot/src/trigger_wasm.rs`, and transport ABI in `crates/chainbot/wit/trigger-plugin.wit` plus repo-root `wit/trigger-plugin.wit`.
- Current runtime model only distinguishes `Process` vs `WasmComponent` at manifest decode and `ExternalTriggerRuntime::{Process, Wasm}` at trigger execution; there is no persistent-session lifecycle metadata yet.
- Current wasm host path is short-lived: create store/instance, call `start`, loop `drain`, call `stop`, drop runtime state; push callbacks and typed durable-ack outcomes do not exist yet.
- Existing tests already cover runtime shape acceptance/rejection and wasm drain-loop behavior in `crates/chainbot/tests/config_loading.rs`, `crates/chainbot/tests/trigger_plane.rs`, and unit tests in `crates/chainbot/src/plugin/contract.rs` / `crates/chainbot/src/trigger_wasm.rs`.
- Added explicit trigger-runtime manifest metadata under `runtime.trigger_runtime` with typed enums for lifecycle, push callback semantics, durable ACK semantics, and host error categories; this keeps lifecycle policy in manifest contract instead of runtime-side conventions.
- Validation now rejects missing trigger runtime lifecycle metadata and process/wasm lifecycle mismatches via stable `ContractError::NodePluginInvalidField` fields (for example `plugin.runtime.trigger_runtime.lifecycle`).
- External-node manifests are now guarded against accidental trigger-runtime metadata (`plugin.runtime.trigger_runtime`), preventing mixed schema declarations across plugin kinds.
- Durable staging insertion point should sit between durable inbox append and `TriggerPlane::normalize_emission()`, so acceptance authority remains inside `TriggerPlane`.
- Existing long-lived ownership precedent already exists in `crates/chainbot/src/ingress/supervisor.rs` and lease control in `crates/chainbot/src/cli.rs`; new external-trigger supervision should compose with those boundaries rather than move session ownership into `TriggerPlane`.
- Recovery hooks already exist across `state_db.rs` / `state.rs` trigger snapshots, checkpoints, and inbox replay; Task 2 should extend persistence APIs instead of inventing a parallel replay channel.
- Added durable pre-accept staging in `state_db.rs` as a dedicated `staged_trigger_event_records` table with deterministic ordering (`staged_at_ms`, `trigger_id`, `staging_id`) so restart reconciliation can enumerate pending external emissions without touching accepted trigger history.
- Kept acceptance authority unchanged: `mark_staged_trigger_event_accepted` only advances staged row state, while authoritative accepted-event writes still flow through `write_trigger_record`/`TriggerPlane::normalize_emission()` paths.
- Deterministic duplicate/conflict behavior is enforced with `UNIQUE (trigger_id, event_id)` and append semantics returning `false` on conflict, ensuring supervisors can retry idempotently without creating ambiguous staged rows.
- Added `crates/chainbot/src/external_trigger_supervisor.rs` as a daemon-owned boundary for external trigger session registry/state/start-stop-reconcile seams; this keeps long-lived session ownership out of `TriggerPlane` acceptance normalization paths.
- Composed the new supervisor in `run_internal_serve_daemon_loop` so daemon reload/error paths reconcile desired external sessions independently from ingress listener ownership and accepted-event processing.
- Task 4 ABI cut introduced transport-only WIT definitions at `wit/trigger-plugin.wit` and `crates/chainbot/wit/trigger-plugin.wit` with a guest-driven `push-trigger-event` host callback result type (`durable-ack` success and typed `backpressure`/`lease-lost`/`shutting-down` errors), avoiding any manifest business-field duplication.
- `crates/chainbot/src/trigger_wasm.rs` now centralizes typed host callback outcomes (`HostPushOutcome`) so retryable backpressure and terminal lease/shutdown states are machine-distinguishable without introducing Task 6+ session ownership or durable staging behavior early.
- Task 6 landed a concrete `WasmTriggerSession` runtime owner in `crates/chainbot/src/trigger_wasm.rs` with explicit runtime identity (`session_id`, `store_id`, `instance_id`) and guest lifecycle bookkeeping (`turn_count`, `last_turn_at_ms`, `last_reconciled_at_ms`), so repeated daemon turns can prove they reuse one long-lived session runtime without leaking ownership into `TriggerPlane`.
- `crates/chainbot/src/external_trigger_supervisor.rs` now attaches `Option<WasmTriggerSession>` directly to daemon-owned `ExternalTriggerSession`; `reconcile` retain paths update reconcile timestamps instead of re-instantiating runtime handles, and stop/reconcile removal drops the owned session state by removing the registry entry.
- Focused lifecycle tests now assert both halves of Task 6 semantics: repeated turns keep the same wasm runtime identity, and stop/reconcile teardown removes old runtime ownership before restart constructs a new identity.
- Task 7 durable-ack bridge now runs through Task 2 persistence API: `ExternalTriggerSupervisor::push_wasm_guest_event` maps guest callback pushes into `RuntimeStateStore::append_staged_trigger_event_record` and only returns `HostPushOutcome::DurableAck` when append returns `Ok(true)`.
- To avoid false ACKs, staged append `Ok(false)` and storage errors are mapped to retryable host backpressure, so callback success semantics mean "durably staged now" rather than "accepted in memory".
- Task 8 callback taxonomy split keeps `DurableAck` semantics unchanged while exposing explicit terminal states at the supervisor boundary: missing/non-owned/non-wasm sessions map to `TerminalLeaseLost`, stopping sessions map to `TerminalShuttingDown`, and persisted-append conflicts remain `RetryableBackpressure`.
- Added source-aware staged-append mapping in `trigger_wasm::host_push_result_from_staged_append` so durable append failures no longer require a single hardcoded error class; callers provide the host error source and guests can distinguish retry/backoff from permanent stop conditions.
- Task 9 stabilized supervisor control-flow semantics by adding explicit push control states (`QueueSaturated`, `BudgetExhausted`, `ShuttingDown`, `LeaseLost`) in `ExternalTriggerSession`; `push_wasm_guest_event` now maps those sources through `trigger_wasm::map_control_flow_source_to_push_outcome` so callback outcomes are machine-distinguishable and deterministic before decode/append work runs.
- Queue saturation and budget exhaustion are now intentionally modeled as retryable backpressure sources, while daemon shutdown and lease loss are terminal sources; focused tests assert each source reaches the callback boundary without collapsing into generic staged-append error branches.
- Reconcile now treats lease loss as a hard stale-session boundary: sessions with owner mismatch or `LeaseLost` control state are marked stopping and removed before desired-state retention, preventing stale runtimes from surviving reconciliation turns.
- Desired-vs-existing session matching now includes wasm component identity (`WasmTriggerSession::component`) in addition to plugin/runtime, so manifest/runtime module changes force deterministic stop+restart instead of silently retaining outdated wasm ownership.
- Focused supervisor tests now cover each reconcile cause independently: disabled trigger removal, lease-loss restart, and wasm component change restart.
- CLI daemon lifecycle now invokes a dedicated `teardown_external_trigger_sessions` helper in `cli.rs` on stop-request detection, lease-conflict serve errors, and final loop unwind; this keeps one daemon-scoped `ExternalTriggerSupervisor` instance lease-bound while making teardown deterministic across shutdown and owner-loss exits.
- Task 12 acceptance bridge now drains `staged_trigger_event_records` inside `TriggerPlane::collect_run_requests_with_progress` and converts each staged row into `TriggerEmission` before calling the existing `normalize_emission()` path, preserving a single accepted-event authority.
- Crash-safe ordering is enforced by marking staged rows accepted only after `normalize_emission()` returns success (`Ok`), so authoritative accepted-event writes (trigger record/snapshot/checkpoint) always happen before staged-row acknowledgement.
- Duplicate replay safety is preserved without a second writer: when a staged row references an already-accepted event, `normalize_emission()` returns `None`, no new accepted record is written, and the staged row is still drained by `mark_staged_trigger_event_accepted`.
- Added focused regression coverage in `tests/trigger_plane.rs` and daemon-turn coverage in `tests/end_to_end_vertical_slice.rs`/`src/cli.rs` tests to prove staged external rows replay through the normal acceptance path and do not double-create accepted trigger records.
- Process-runtime convergence landed at orchestration level: `run_internal_serve_daemon_loop` now routes process external triggers through the daemon-owned `ExternalTriggerSupervisor` session registry and stages process emissions before acceptance, so process and wasm share one top-level session lifecycle surface.
- `TriggerPlane` now supports an acceptance-only mode (`open_with_store_acceptance_only`) that disables direct external process polling while keeping staged-row drain, normalization, dedup/cooldown, checkpoint, and replay suppression semantics unchanged.
- The process adapter path reuses existing trigger-plugin protocol execution (`collect_external_process_trigger_emissions`) and writes `staged_trigger_event_records`; accepted-event authority remains single-path in `TriggerPlane::normalize_emission` with no duplicate accepted/replay writers.
- Added explicit Wasmtime store limiter wiring in `WasmTriggerSession` using `Store::limiter` + `StoreLimitsBuilder`; sessions now carry explicit `WasmTriggerSessionConfig` defaults and support deterministic limiter override tests.
- Added per-cycle fairness planning in `ExternalTriggerSupervisor` via `ExternalTriggerSupervisorBudget` (`max_sessions_per_cycle`, `max_polls_per_session`) with round-robin active-session rotation; non-selected budget-managed sessions are marked `BudgetExhausted`, so callback outcomes reuse existing typed retryable backpressure taxonomy.
- `serve_once_with_lease` now consumes supervisor-provided process poll budgets, and process staging loops obey explicit poll quotas per selected session; this bounds hot-session work per daemon cycle without changing `TriggerPlane` acceptance authority.
- Added trigger-plane regression coverage for disable/removal replay safety: `disabled_or_removed_trigger_does_not_accept_future_staged_rows_after_restart` proves staged rows are not accepted once a trigger is disabled or removed, and accepted-record cardinality remains unchanged.
- Added end-to-end regression coverage for restart/teardown paths in `end_to_end_vertical_slice.rs`: trigger disable prevents post-restart acceptance, lease-owner loss teardown blocks future accepted events, and live plugin-manifest switch (`trigger-e2e-plugin` -> `trigger-e2e-plugin-v2`) yields exactly one new accepted event for the new session path.
- Deterministic lease-loss e2e assertions must account for daemon lease renewal cadence (`SERVE_LEASE_RENEW_INTERVAL_MS = 10_000`), so wait windows for lease-conflict propagation need to exceed the renewal interval.
- Task surface: split trigger discovery/help/catalog wording by process vs persistent wasm lifecycle. Added `lifecycle` field to `PluginDetail` in `catalog.rs` with render for both text and JSON output. Updated `cli.rs` catalog help to mention lifecycle distinction. Added `catalog_show_reports_wasm_trigger_lifecycle_in_json_and_text` and `catalog_show_reports_process_trigger_lifecycle_in_json_and_text` tests to `catalog_surface.rs`. Added `help_catalog_distinguishes_trigger_lifecycle_models` test to `cli_surface.rs`. Updated `examples/README.md` to clarify the two lifecycle models in plugin-integrations.
- `PluginDetail` now has optional `lifecycle: Option<String>` field populated for external triggers from `manifest.trigger_runtime.lifecycle`, returning `process_short_lived` or `wasm_daemon_persistent_session` strings for display.
- Text rendering for lifecycle includes human-readable descriptions: "Short-lived process adapter: poll-based, supervisor-orchestrated" for process and "Daemon-persistent wasm session: long-lived Wasmtime ownership" for wasm.
- Fixed example-surface alignment: `examples/plugin-integrations/plugins/market-trigger-plugin/config.toml` now includes `[trigger_runtime]` with `lifecycle = "process_short_lived"` so `chainbot catalog show plugin:market-trigger-plugin` actually displays lifecycle as the README promises.
- Added retryable vs terminal host outcome wording to catalog help Outputs: "wasm trigger plugins show host callback outcomes: retryable backpressure (queue_saturated/budget_exhausted) vs terminal lease_lost/shutting_down".
- Fixed catalog show wasm protocol surface: for `wasm_daemon_persistent_session` triggers, `build_plugin_detail` now sets `protocol: None` and the renderer shows "Host callback protocol" with daemon-persistent session semantics instead of the process-oriented start/event message contract.
- Matrix completion pass added deterministic multi-surface coverage: `trigger_plane.rs` now proves acceptance-only mode drains known staged rows while preserving unknown rows for future reconcile, preventing false-positive acceptance under mixed trigger sets.
- `runtime_state_parity.rs` now verifies restart reconcile order for interleaved staged rows across multiple trigger IDs stays durable and deterministic (`staged_at`-ordered), then confirms partial ACK leaves only the expected pending residue.
- `state_runtime_persistence.rs` now includes an expiry-transfer lease scenario across store reopen, proving stale owner turnover remains deterministic without requiring manual release.
- `end_to_end_vertical_slice.rs` now exercises a realistic multi-process-trigger fixture path by installing a second trigger package at runtime, asserting both sessions accept once and remain duplicate-safe after a serve restart.
- `external_trigger_supervisor` unit coverage now includes bounded stress fairness rotation over repeated cycles and a control-matrix test that walks queue saturation -> durable ACK -> lease loss -> reconcile recovery using JSON-like wasm push payloads.
- Final hardening sweep: daemon loop teardown paths were drifted into both early-return branches and the loop epilogue; consolidating stop/conflict exits onto the epilogue keeps one surviving session-teardown model while preserving mid-loop recovery teardown for reload-failure retries.
- Final-wave fix: production daemon now stages wasm persistent trigger events through `ExternalTriggerSupervisor::push_wasm_guest_event` inside `serve_once_with_lease`, so host-callback durable append runs in the real serve path instead of unit-test-only call sites.
- Runtime host validation no longer hard-requires `plugin.executable` for wasm persistent external triggers; `trigger::validate_trigger_plugin_manifest` now branches by lifecycle (`Process` requires executable, `WasmDaemonPersistentSession` does not).
- Production callback staging remains single-path: wasm callback rows are persisted as `staged_trigger_event_records` and then accepted only through `TriggerPlane::normalize_emission()` in acceptance-only mode, preserving one acceptance authority.
- Added deterministic e2e proof (`serve_turn_accepts_wasm_persistent_external_trigger_via_daemon_callback_path`) that serve daemon accepts `external-trigger-e2e:wasm-turn-1` through the real callback bridge and persists it as accepted trigger history.
- Shipped examples now truthfully expose both external trigger lifecycle models in `examples/plugin-integrations/` by adding `market-trigger-wasm-plugin` plus `tr-market-feed-wasm`, and catalog plugin-count expectations were updated accordingly.
- Final-wave blocker fix: `WasmTriggerSession` now prefers loading an existing configured wasm module file (resolved from plugin manifest-relative module path), wasm callback staging in `serve` no longer hard-stops at turn 1, and `examples/plugin-integrations` is runnable with executable process scripts plus a committed `market-trigger-wasm-plugin/bin/external_trigger.wasm` artifact.
- Strict contract cleanup: `external_trigger` manifests now require explicit `trigger_runtime.lifecycle` (missing `trigger_runtime` no longer falls back to implicit process), and trigger-plane test manifests were updated to include explicit `process_short_lived` runtime metadata.
- F4 re-review evidence: daemon `serve_once_with_lease` now calls `stage_wasm_external_trigger_sessions` in production, `catalog show plugin:market-trigger-wasm-plugin` renders host-callback protocol text instead of process `start_message/event_message`, and `CHAINBOT_CONFIG_DIR=examples/plugin-integrations chainbot validate` passes with both lifecycle examples present.
- F3 manual-QA evidence: on a clean temp copy of `examples/plugin-integrations` (with no preexisting runtime DB), `target/debug/chainbot help catalog`, `catalog show plugin:market-trigger-plugin`, and `catalog show plugin:market-trigger-wasm-plugin` now expose the intended lifecycle split and wasm host-callback semantics in observable CLI output.
- F3 runtime evidence: starting `target/debug/chainbot serve` against the clean temp example produced fresh persisted runs from both `tr-market-feed` and `tr-market-feed-wasm`; `status` showed `serve: active`, `observe` showed accepted wasm trigger events plus started/finished workflow logs, and `stop` returned the root to `serve: idle`.
- Final-wave blocker continuation: serve-time wasm staging now requests bytes from `ExternalTriggerSupervisor::execute_wasm_guest_turn`, which drives `WasmTriggerSession::execute_guest_turn` (guest export call) before durable callback staging; host-side `build_wasm_guest_transport_envelope` synthesis was removed from the daemon path.
- Callback-fidelity cut: `WasmTriggerSession::execute_guest_turn` now requires `run-session` to invoke `trigger-host.push-trigger-event`, the export/seed fallback bridge was removed, and wasm envelope decode-to-staged-record glue now lives in `external_trigger_supervisor.rs` via `stage_wasm_guest_turn` so CLI only orchestrates the session turn.
