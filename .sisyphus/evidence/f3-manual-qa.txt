# F3 Real Manual QA

All commands run against the current workspace at `seville-v1`.
Exit code 0 unless noted.

## Build

```
cargo build -p chainbot
```
Exit 0. Binary produced at `target/debug/chainbot`.

## MCP Plugin Host Integration Tests

### stdio happy path

```
cargo test -p chainbot --test mcp_plugin_host stdio_tool_call_happy_path -- --exact
```
Exit 0. One MCP initialize, one tool call, one teardown per invocation. No session reuse.

### HTTP happy path

```
cargo test -p chainbot --test mcp_plugin_host http_tool_call_happy_path -- --exact
```
Exit 0. Streamable HTTP transport with static auth header, initialize, tool call, and teardown succeed.

### HTTP 401 maps to plugin error

```
cargo test -p chainbot --test mcp_plugin_host http_401_maps_to_plugin_error -- --exact
```
Exit 0. 401 response normalized to a deterministic plugin execution error with no credential leakage in the error message.

### HTTP stale session recovers or fails deterministically

```
cargo test -p chainbot --test mcp_plugin_host http_stale_session_recovers_or_fails_deterministically -- --exact
```
Exit 0. Stale `Mcp-Session-Id` triggers transparent reinitialization; if recovery fails, a deterministic error is returned (no panic, no zombie state).

### HTTP non-MCP endpoint rejected

```
cargo test -p chainbot --test mcp_plugin_host http_non_mcp_endpoint_rejected -- --exact
```
Exit 0. Non-MCP response body is rejected with a deterministic plugin error before any tool execution.

## Runtime Execution Integration

### MCP tool entrypoint routing

```
cargo test -p chainbot runtime_execution_routes_mcp_tool_entrypoint -- --exact
```
Exit 0. Workflow node with `entrypoint = "mcp.tool.v1"` routes through the MCP invoker path. No legacy `executable` field required.

### Legacy external node dispatch preserved

```
cargo test -p chainbot runtime_execution_preserves_legacy_external_node_dispatch -- --exact
```
Exit 0. `node.exec.v1` plugins continue through the legacy subprocess path; workflow report semantics (`status`, `schedule_waves`, `node_states`, `node_outputs`, `runtime_namespaces.run_scoped`) are unchanged.

## Catalog and CLI Surface Tests

```
cargo test -p chainbot --test catalog_surface
```
Exit 0. All catalog surface tests pass.

```
cargo test -p chainbot --test cli_surface
```
Exit 0. All CLI surface tests pass.

## CLI Operator Checks

### catalog list --kind plugin

```
cargo run -p chainbot -- catalog list --kind plugin
```
Exit 0. Installed MCP plugins appear in output with transport detail. Non-MCP plugins remain unchanged.

### catalog show plugin:mcp-http-plugin

```
cargo run -p chainbot -- catalog show plugin:mcp-http-plugin
```
Exit 0. Output includes MCP transport type, declared operations, and package metadata. No direct-connect commands surfaced.

### help plugin

```
cargo run -p chainbot -- help plugin
```
Exit 0. Help output preserves package-centric model; no raw server connection commands exposed. MCP details are additive and scoped to installed plugin descriptions.

## Full Test Suite

```
cargo test -p chainbot --test mcp_plugin_host
cargo test -p chainbot --test node_plugin_host
cargo test -p chainbot --test catalog_surface
cargo test -p chainbot --test cli_surface
```
All exit 0. No regressions in non-MCP surfaces.
