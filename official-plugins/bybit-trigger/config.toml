manifest_version = "2.0.0"
plugin_id = "bybit-trigger"
kind = "external_trigger"
entrypoint = "trigger.exec.v1"
capabilities = ["trigger.listen.event"]
executable = "bin/bybit-trigger"

[activation]
optional_secret_slots = ["api_key", "api_secret"]
requires_allowed_origins = true

[trigger_runtime]
lifecycle = "process_daemon_session"
push_callback = "inline_response"
durable_ack = "after_store_persist"
host_error_categories = ["transport", "protocol_contract", "plugin_fatal"]

[event_schema]
summary = "Bybit listener payload"
fields = ["exchange", "product_line", "listener_kind", "stream", "event_id", "payload"]
listener_modes = ["event_log", "state_change"]

[source]
manifest_version = "1.0.0"
install_mode = "build_required"
runtime = "bin"
entry_artifact = "bin/bybit-trigger"
release_version = "0.1.0"

[source.build]
kind = "cargo"
command = ["cargo", "build", "--release", "--bin", "bybit_trigger_official_plugin"]
workdir = "crate"

[[source.build.outputs]]
from = "crate/target/release/bybit_trigger_official_plugin"
to = "bin/bybit-trigger"
